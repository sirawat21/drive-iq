name: Task-2 build
run-name: Task 2 - Testing build by ${{ github.actor }}
on:
  push:
    branches: [task-2]
jobs:
  task2:
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v2

      - name: Create Node environment
        uses: actions/setup-node@v1
        with:
          node-version: 12.x

      - name: Install dependencies
        run: npm install

      - name: Test build
        run: npm run build